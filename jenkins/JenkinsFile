pipeline {
    agent any

    stages {
        stage('Unit Test Backend') {
            steps {
                dir ('bug-tracker-backend') {
                    go test -v ./...
                }
            }
        }
    }
}